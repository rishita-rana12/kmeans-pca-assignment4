This project combines PCA and K-Means clustering.

Clustering is performed before and after PCA.
Adjusted Rand Index (ARI) is used to evaluate performance.

The comparison shows how dimensionality reduction affects clustering.
